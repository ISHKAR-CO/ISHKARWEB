<div class="home-banner-two" data-section-id="{{ section.id }}" data-section-type="home-banner-two">
  <div class="wc_img_brn_two image_banner banner-height-medium lazyload"
       data-bgset="{% include 'bgset', image: section.settings.image %}"
       data-sizes="auto"
       data-parent-fit="cover"
       style="background-position: {{ section.settings.alignment }}; background-image: url('{{ section.settings.image | img_url: '300x300' }});">
    <div class="banner-header large-down--hide"></div>
  <div class="banner-caption d-flex flex-wrap">
   
    {%- for block in section.blocks -%}
    {%- if block.type == "text" -%}
    <div class="inner_caption {{ block.type }} d-flex flex-wrap align-items-center justify-content-center">
      {%- if block.settings.link != blank -%}<a class="full-width-link" href="{{ block.settings.link }}"></a>{%- endif -%}
      <div class="wrapper">
        <h3 class="s-subtitle">{{ block.settings.subtitle }}</h3>
        <h2 class="s-title h2-large">
          {{- block.settings.title -}}</h2>
        {%- if block.settings.content != blank -%}
        <div class="bnr_small_content">
          <p>{{block.settings.content}}</p>
        </div>
        {%- endif -%}
      </div>
    </div>
    {%- endif -%}
    {%- if block.type == "text-border" -%}
    <div class="inner_caption {{ block.type }} d-flex flex-wrap align-items-center justify-content-center">
      <div class="wrapper">
        <div class="text-border-title">{{ block.settings.title }}</div>
      </div>
    </div>
    {%- endif -%}
    {%- endfor -%}
      
  </div>
    </div>
</div>

{% schema %}
  {
    "name": "Home banner 2",
	"class":"index--section index-section--flush",
	"max_blocks":4,
    "settings": [
		{
			"type":"image_picker",
			"id":"image",
			"label":"Upload Image"
		}
	],
	"blocks":[
	  {
		"type":"text",
		"name":"Text",
		"settings":[
		  {
			"type":"text",
			"id":"subtitle",
			"label":"Enter title"			
		  },
		  {
			"type":"text",
			"id":"title",
			"label":"Enter title"
		  },
		  {
			"type":"url",
			"id":"link",
			"label":"Provide link"
		  },
		  {
			"type":"textarea",
			"id":"content",
			"label":"Content"
		  }
		]
	  },
	  {
		"type":"text-border",
		"name":"Text with border",
		"settings":[
		  {
			"type":"textarea",
			"id":"title",
			"label":"Enter detail"			
		  }
		]
	  }
	],
	"presets":[
		{
		  "name":"Home banner 2",
		  "category":"Image",
		  "blocks":[
			{
			  "type":"text-border",
			  "settings":{
				   "title":"Our mission is to restore pathways to some of the world’s most extraordinary sites, objects and cultures."
			  }			   
			},
			{
			  "type":"text",
			  "settings":{
				   "title":"TRAVEL",
				   "subtitle":"Explore our",
				   "link":"#"
			  }
			},
			{
			  "type":"text",
			  "settings":{
				   "title":"OBJECTS",
				   "subtitle":"Shop our",
				   "link":"#"
			  }  
			},
			{
			  "type":"text",
			  "settings":{
				   "title":"STORIES",
				   "subtitle":"Discover our",
				   "link":"#"
			  }  
			}
		  ]
		}
	]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
